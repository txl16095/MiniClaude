/**
 * Analytics 配置 - 占位文件
 * 原始功能已删除，保留此文件以保持兼容性
 */

export function isAnalyticsDisabled(): boolean {
  return true
}

export function isFeedbackSurveyDisabled(): boolean {
  return true
}

export function getAnalyticsConfig(): Record<string, unknown> {
  return {}
}
